# Generated artifacts

This directory is populated by the deterministic local pipeline and automated tests.

Typical outputs include model packages, validation metrics, predictions, monitoring results, figures, and the local SQLite serving database. Generated artifacts are ignored by Git so the repository stays lightweight and reproducible.
