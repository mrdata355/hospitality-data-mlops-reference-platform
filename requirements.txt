# Exact direct dependencies used by the credential-free local and CI path.
# Dependabot proposes controlled upgrades; CI regenerates evidence after every change.
pandas==2.2.3
numpy==2.3.5
scikit-learn==1.8.0
joblib==1.5.3
fastapi==0.139.2
uvicorn==0.48.0
pydantic==2.13.4
PyYAML==6.0.3

# Validation dependencies used by the credential-free CI path.
pytest==9.0.3
httpx==0.28.1
