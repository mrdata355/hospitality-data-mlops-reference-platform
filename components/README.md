# Platform Components

The component directories divide the flagship platform into six independently reviewable engineering domains while preserving shared contracts, data models, deployment controls, and observability.

Each component README declares ownership, input and output interfaces, grain, operational controls, and its role in the end-to-end platform.
