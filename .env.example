# Credential-free local validation. The deterministic pipeline generates the model artifact.
MODEL_SOURCE=local
MODEL_ALIAS=Champion

# Managed deployment option. Never place live secrets in this file or commit `.env`.
# MODEL_SOURCE=mlflow
# MODEL_URI=models:/hospitality_data_platform.models.member_churn_model@Champion

# Environment-scoped runtime configuration.
PLATFORM_CATALOG=hospitality_data_platform_dev
AS_OF_MONTH=2026-07-01
MAX_FORECAST_WAPE=0.30
REQUIRE_BASELINE_IMPROVEMENT=true
