# Databricks Deployment Assets

This directory contains the Databricks Asset Bundle, environment-specific catalog variables, workflow definitions, model promotion logic, batch scoring, monitoring, and rollback controls.

The local repository path remains credential-free. Databricks deployment begins only after an approved workspace, identity, catalog permissions, source volumes, and secret configuration are available.
