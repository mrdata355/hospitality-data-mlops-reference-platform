# Local Execution Scripts

The scripts directory provides deterministic, credential-free entry points for validation and cleanup.

- `run_all.py` generates synthetic sources, builds the data platform, creates features, trains models, evaluates acceptance gates, and writes monitoring outputs.
- `clean_outputs.py` resets generated data and artifacts for a clean replay.
