# Databricks SQL Assets

The SQL assets implement parameterized Unity Catalog setup, Auto Loader ingestion support, Silver MERGE logic, dimensional modeling, Gold marts, point-in-time feature tables, data-quality results, and forecast monitoring.

Catalog names are supplied at runtime so development, staging, and production remain isolated without source-code edits.
